name: CI Hello Test

on:
  push:
    branches: [ feature/**, main ]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Print hello
        run: |
          echo "CI Hello Test: runner available"
          uname -a || true
